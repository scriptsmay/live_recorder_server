# TODO

## 📋 TODO: 集成多插件直播录制系统

### 1. 架构设计与环境准备

- [ ] **依赖调研**：确认 `stream-gears` 在当前环境（Node.js + Linux/ARM64）的兼容性。
- [ ] **安装依赖**：在后端项目中安装 `stream-gears` 及相关类型定义。
- [ ] **配置架构设计**：在全局配置表新增字段，支持在全局配置中指定 `downloader` 类型。
- [ ] **并发管理方案**：由于录制是长耗时 IO 任务，设计基于 `worker_threads` 或 `child_process` 的隔离方案，避免阻塞 Node.js 事件循环。

### 2. 核心模块实现

- [ ] **定义下载器接口 (Interface)**：
- 定义统一的 `start()`, `stop()`, `getStatus()` 方法。

- [ ] **实现 Stream-Gears 插件**：
- 封装 `stream-gears` 官方库。
- 实现 FLV 流的自动 Tag 修复逻辑。
- 增加**异常重连机制**：当下载器抛出错误或数据流中断时，触发指数退避重试。

- [ ] **保留 FFmpeg 插件 (Fallback)**：
- 将现有的 `ffmpeg` 命令封装为与新接口兼容的插件。
- 作为 `stream-gears` 无法处理特定流时的备选方案。

### 3. 业务逻辑集成

- [ ] **任务工厂 (Downloader Factory)**：
- 根据全局配置（如 `platform: 'bilibili'` 或 `downloader: 'stream-gears'`）动态实例化对应的插件。

- [ ] **状态监控与日志**：
- 实时记录录制速率、已下载文件大小。
- 捕获 `stream-gears` 的底层日志并输出到项目日志系统，方便排查花屏修复记录。

### 4. 前端与交互

- [ ] **配置界面更新**：
- 在全局设置页面增加“下载插件”下拉菜单（选项：`stream-gears (推荐)`、`ffmpeg`）。

- [ ] **状态可视化**：
- 在前端展示当前录制任务正在使用的下载引擎。

### 5. 测试与优化

- [ ] **稳定性测试**：模拟国内直播间断流、网络波动，验证 `stream-gears` 的自动修复效果。
- [ ] **资源审计**：对比录制同等质量流时，`stream-gears` 方案与 `ffmpeg` 进程的 CPU/内存占用情况。

---

## 🛠 AI 执行提示 (Prompt Hint)

> "请基于 Node.js 环境，参考工厂模式重构录制模块。优先使用 `stream-gears` 库处理 FLV 流，并确保在任务崩溃时有完善的重连逻辑。注意：录制过程需在独立线程运行，不要阻塞主进程。"
